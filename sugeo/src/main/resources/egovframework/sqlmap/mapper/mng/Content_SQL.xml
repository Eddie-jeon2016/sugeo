<?xml version="1.0" encoding="UTF-8"?><!--Converted at: Wed Feb 01 15:11:03 KST 2017-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ContentDAO">

 	<select id="ContentDAO.selectContentList" parameterType="ComDefaultVO" resultType="ContentVO">
 		
			SELECT 
				CONTENT_ID, CONTENT_NAME , CONTENT,USE_YN, REG_DATE, REG_ID, MOD_DATE, MOD_ID
			FROM 
				CONTENT_MAPPING
			WHERE 1=1 
 		      AND USE_YN = 'Y'

			ORDER BY REG_DATE DESC
			LIMIT #{recordCountPerPage} OFFSET #{firstIndex} 
							
 	</select>
 	
 	<select id="ContentDAO.selectContentListCnt" resultType="int">
 		
			SELECT COUNT(CONTENT_ID)
			FROM 
				CONTENT_MAPPING
			WHERE 1=1 
 		      AND USE_YN = 'Y'
	
 	</select>
 	
 	<insert id="ContentDAO.insertContent" parameterType="ContentVO">
 		INSERT INTO CONTENT_MAPPING 
 					(
 					CONTENT_ID
 					,CONTENT_NAME
 					,CONTENT
 					,USE_YN 
 					,REG_DATE
 					,REG_ID 
 					) 
 				VALUES
 					(
 				    #{contentId}
 				   ,#{contentName}
 				   ,#{content}
 				   ,'Y'
 				   ,SYSDATE()
 				   ,0	  
 				   )
 	
 	</insert>
 	
 	<update id="ContentDAO.updateContent" parameterType="ContentVO">
 		UPDATE CONTENT_MAPPING
 			SET CONTENT_NAME = #{contentName}
 				,CONTENT = #{content}
 				,MOD_DATE = SYSDATE()
 				,MOD_ID = 0
 			WHERE CONTENT_ID = #{contentId}
 	</update>
 	
 	<select id="ContentDAO.selectContentView" parameterType="ContentVO" resultType="ContentVO">
 		SELECT  	CONTENT_ID
 					,CONTENT_NAME
 					,CONTENT
 					,USE_YN 
 					,REG_DATE
 					,REG_ID 
 					,MOD_DATE
 					,MOD_ID
 					FROM CONTENT_MAPPING
 					WHERE CONTENT_ID = #{contentId}
 	</select>
</mapper>